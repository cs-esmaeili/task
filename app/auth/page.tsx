import LoginForm from '@/components/LoginForm';
import React from 'react';

const page = () => {
    return (
        <div>
            this is auth page
            <LoginForm />
        </div>
    );
};

export default page;